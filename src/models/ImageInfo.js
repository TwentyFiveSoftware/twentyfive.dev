export default class ImageInfo {
    constructor(fontAwesomeIcon, image = null, imageStyle = {}) {
        this.fontAwesomeIcon = fontAwesomeIcon;
        this.image = image;
        this.imageStyle = imageStyle;
    }
}
